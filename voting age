#include<stdio.h>
#include<conio.h>
main()
{
clrscr();
int age;
printf("Enter age:");
scanf("%d",&age);
if(age>=18)
{
printf("You are eligible:");
}
else
{
printf("You are not eligible:);
}
getch();
return 0;
}
